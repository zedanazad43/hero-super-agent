# Hero Super Agent — environment configuration (copy to .env and fill in)
PORT=3001
AIMASTER_ENDPOINT=http://127.0.0.1:8000

# ── UltimateArbitrageHFT integration ──
# Public API base of the arbitrage Cloudflare Worker
ARBITRAGE_API=https://api.ecostamp.net
# Admin token for the arbitrage Worker (x-admin-token header)
ARBITRAGE_ADMIN_TOKEN=
